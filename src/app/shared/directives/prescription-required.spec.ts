import { PrescriptionRequired } from './prescription-required';

describe('PrescriptionRequired', () => {
  it('should create an instance', () => {
    const directive = new PrescriptionRequired();
    expect(directive).toBeTruthy();
  });
});
