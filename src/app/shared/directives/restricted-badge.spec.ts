import { RestrictedBadge } from './restricted-badge';

describe('RestrictedBadge', () => {
  it('should create an instance', () => {
    const directive = new RestrictedBadge();
    expect(directive).toBeTruthy();
  });
});
