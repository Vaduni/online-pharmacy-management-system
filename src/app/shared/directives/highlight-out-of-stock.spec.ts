import { HighlightOutOfStock } from './highlight-out-of-stock';

describe('HighlightOutOfStock', () => {
  it('should create an instance', () => {
    const directive = new HighlightOutOfStock();
    expect(directive).toBeTruthy();
  });
});
