import { MedicineSummaryPipe } from './medicine-summary-pipe';

describe('MedicineSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicineSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
